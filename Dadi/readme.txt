Generare un numero random da 1 a 6, sia per il giocatore sia per il computer. Stabilire il vincitore, in base a chi fa il punteggio più alto.

1// Generare un numero casuale da 1 a 6 
    1.1//Compiere questo passaggio per l'utente e per il computer

2// Determinare il vingitore in base al numero più alto
    2.1// IF il numero dell'utente è più alto, mostrare 'Hai vinto!'
    2.2// IF ELSE IF il numero dell'computer è più alto, mostrare 'Hai perso'
    2.3// ELSE i numeri sono identici 'Pareggio'

 